# TO-DO LIST

A simple To-Do List application built with **Vanilla JavaScript**, HTML, and CSS.  
This app allows users to add, delete, and mark tasks as completed, with persistent storage using `localStorage`.

---

## 🔹 Features

- Add new tasks
- Delete tasks
- Toggle tasks as completed
- Count of pending tasks
- Tasks persist after page reload
- Responsive design for desktop and mobile

---

## 💻 How to Use

1. Open `index.html` in your browser.
2. Enter a task in the input field.
3. Click **Add** to add the task to the list.
4. Click on a task to mark it as completed.
5. Click the **trash icon** to delete a task.
6. The pending task count is displayed at the top.

---

## 🛠️ Technologies Used

- HTML5
- CSS3 / Tailwind (if used)
- JavaScript (ES6)
- localStorage for data persistence

---
## 🌐 Live Demo

Once deployed via GitHub Pages, your app will be accessible at:

https://ramcharankhv-byte.github.io/TO-DO-LIST/

TO-DO-LIST/
├── index.html # Main HTML file
├── script.js # JavaScript for interactivity
├── input.css # Optional input styling
├── output.css # Compiled Tailwind CSS (if used)
├── README.md # This file
├── package.json # Node / Tailwind config (optional)
└── package-lock.json

## 📌 Notes

- Clicking a task marks it as **completed**.
- Completed tasks are shown with a different background and text color.
- Pending tasks are counted and displayed in **blue** at the top.
- All tasks are stored in `localStorage`, so they persist on refresh.

---

## 🚀 Future Improvements

- Add “Edit task” functionality
- Add a progress bar for completed tasks
- Add a “Clear All Completed” button
- Drag & drop to reorder tasks



